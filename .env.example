# MQTT Configuration
# Set to "true" to enable MQTT publishing, "false" to disable
MQTTACTIVE=true

# MQTT Broker Configuration
# Hostname or IP address of your MQTT broker
MQTTHOST=192.168.1.100

# MQTT broker port (default: 1883 for non-TLS, 8883 for TLS)
MQTTPORT=1883

# MQTT topic prefix (will be prepended to all topics)
# Recommended to include trailing slash
MQTTTOPIC=home/heating/

# MQTT Authentication (leave empty if not required)
MQTTUSER=mqtt_user
MQTTPASSWORD=mqtt_password

# Reading Configuration
# Interval in seconds between reading values from the heating system
INTERVAL=60

# Commands to Read
# Comma-separated list of commands to read from your heating system
# Available commands depend on your heating system model and vito.xml configuration
# 
# Common commands:
# - getTempA: Outside temperature
# - getTempWWist: Hot water actual temperature
# - getTempWWsoll: Hot water setpoint temperature
# - getTempVL: Flow temperature
# - getTempRL: Return temperature
# - getBetriebArtHK1: Operating mode heating circuit 1
# - getStatusVerdichter: Compressor status (heat pump)
# - getPumpeStatusHK1: Circulation pump status heating circuit 1
# - getJAZ: Seasonal performance factor
# - getNeigungHK1: Heating curve slope heating circuit 1
#
# Example configuration:
COMMANDS=getTempA,getTempWWist,getTempWWsoll,getTempVL,getTempRL,getBetriebArtHK1,getStatusVerdichter,getPumpeStatusHK1,getJAZ
